namespace System.Windows.Forms
{
    /// <summary>
    /// Defines the edge to render the title bar on for the <see cref="CollapsibleGroupBox"/>
    /// </summary>
    public enum CollapsibleGroupBoxTitleEdge
    {
        //enum values are used to create a pseudo group of binary values
        //for horizontal and vertical values


        Top = 1,
        Bottom = 3,
        Left = 4,
        Right = 12
    }
}
